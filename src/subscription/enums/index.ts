export enum SubscriptionPlan {
  BASIC = 'basic',
  PREMIUM = 'premium',
  VIP = 'vip',
}

export enum PaymentFrequency {
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  PENDING = 'pending',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
}
